# CLAUDE.md

This file exists for Claude Code compatibility. Full project instructions
live in [`AGENTS.md`](AGENTS.md) — read that instead; it covers what this
repo is, the rollout plan, ground truth for ruleset decisions, and testing.

Global instructions from `~/.claude/CLAUDE.md` (commit message conventions,
etc.) still apply on top of `AGENTS.md`.
